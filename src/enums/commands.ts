export enum EUserActions {
  Send = 'send',
  Attack = 'attack',
  Choose = 'choose',
  Help = 'help',
  Show = 'show',
  Leave = 'leave',
  Create = 'create',
  Clear = 'clear',
  Exit = 'exit',
}

export enum EShowOptions {
  Races = 'races',
  Players = 'players',
  Profile = 'profile',
}

export enum EFightAction {
  Attack = 'attack',
}

export enum EFightStatus {
  Win = 'win',
  Lose = 'lose',
  Ongoing = 'ongoing',
}
