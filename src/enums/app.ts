// eslint-disable-next-line import/prefer-default-export
export enum EThemes {
  Dark = 'dark',
  Light = 'light',
}

export enum EAppState {
  Active = 'active',
  SemiActive = 'semiActive',
  Inactive = 'inactive',
}
