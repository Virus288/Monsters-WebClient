import React from 'react';

const AuthLoader: React.FC = () => {
  return <div className="h-screen w-full bg-dark-2 flex justify-center items-center flex-col gap-5" />;
};

export default AuthLoader;
